"""Router de veículos — endpoints a implementar nas próximas iterações."""
from fastapi import APIRouter

router = APIRouter()

# Endpoints a implementar:
# POST   /veiculos          → cadastrar veículo
# GET    /veiculos/{id}     → obter veículo por ID
# GET    /veiculos          → listar veículos (filtro por cliente)
# PATCH  /veiculos/{id}     → atualizar dados do veículo
# DELETE /veiculos/{id}     → remover veículo
